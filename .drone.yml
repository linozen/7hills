---
kind: pipeline
type: docker
name: 7hills

steps:
- name: deploy
  image: docker/compose:1.29.1
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker-compose build --pull
  - docker-compose up -d


volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock

...
